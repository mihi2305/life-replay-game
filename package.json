{
  "name": "life-replay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && python3 -m http.server 5173",
    "start": "npm run build && python3 -m http.server 5173",
    "build": "node scripts/generate-supabase-config.js && node --check game.js",
    "check": "node --check game.js && node --check supabaseClient.js && node --check scripts/generate-supabase-config.js"
  }
}
